# start uvicorn with main project version

uvicorn main:app --reload

# start uvicorm with test file

uvicorn test_main:app --reload

# start virtual environement

source venv/bin/activate

# create virtual environement

python3 -m venv venv

